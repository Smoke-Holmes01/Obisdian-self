# 9_01-白盒-基本路径测试-逻辑覆盖设计测试用例

基本路径测试在不能做到所有路径覆盖的前提下，如果某一程序的每一个独立路径都被测试过， 那么可以认为程序中的每个语句都已经检验过了， 即达到了语句覆盖。 这种测试方法就是通常所说的基本路径测试方法。
基本路径测试方法是在控制流图的基础上，通过分析控制结构的环形复杂度，导出执行路径的基本集合，再从该基本集设计测试用例。
控制流图（可简称流图）是对程序流程图进行简化后得到的，它可以更加突出的表示程序控制流的结构。 控制流图中包括两种图形符号:节点和控制流线。[节点由带标号的圆圈表示，可代表一个或多个语句、一个处理框序列和一个条件判定框（假设不包含复合条件。对于复合条件，则可将其分解为多个单个条件，并映射成控制流图。）、多条流线的汇合点。 控制流线由带箭头的弧或线表示， 可称为边。 它代表程序中的控制流。
通常有名字。]

1.常见结构的控制流图：

其中，包含条件的节点被称为判定节点（也叫谓词节点），由判定节点发出的边必须终止于某一个节点，由边和节点所限定的范围被称为区域。

在选择或是多分支结构中分支的汇聚处，即使没有执行语句也应该添加一个结点

控制流图-复合条件分解

2.环形复杂度： 也称为圈复杂度， 它是一种为程序逻辑复杂度提供定量尺度的软件度量。
环形复杂度的应用——可以将环形复杂度用于基本路径方法，它可以提供:程序基本集的独立路径数量;确保所有语句至少执行一次的测试数量的上界。

3.独立路径是指程序中至少引入了一个新的处理语句集合或一个新条件的程序通路。 采用流图的术语，即独立路径必须至少包含一条在本次定义路径之前不曾用过的边。
程序的环形复杂度是程序基本路径集合中的独立路径条数。
测试可以被设计为基本路径集的执行过程，但基本路径集通常并不唯一。

4. 练习

1. 程序:
void Sort ( int iRecordNum，int iType )
1  { 2  int x=0;
3  int y=0;
4  while ( iRecordNum>0 )
5  { 6   If ( iType==0 )
7    x=y+2;
8   else 9    If ( iType==1 )

10     x=y+10;
11    else 12     x=y+20;
13   } 14  } （1）给以上代码画出控制流图（2）控制流图的环复杂度 V(G)，写出独立路径 （2）V(G)=3+1=4 路径 1：4→14        路径 2：4→6→7→13→4→14 路径 3：4→6→9→10→13→4→14    路径 4：4→6→9→12→13→4→14

2. 使用基本路径测试方法测试以下程序段。
Int IsLeap(int year)
{
    if(year % 4 == 0)                  ①
    {
       if(year % 100 == 0)             ②
        {
           if(year % 400 == 0)         ③ leap = 1;             ④ else leap = 0;            ⑤
        }
        else leap = 1;                 ⑥
    }
    else leap = 0;                   ⑦ return leap;                     ⑧
}
(1)画出程序的控制流图(用题中给出的语句编号表示)。
(2)计算上述程序段的环形复杂度。
(3)导出基本路径集，列出程序的独立路径。
[   (1)方法:如何画控制流图？
① 确定流程，拆分复合条件；
② 将条件标注在线上，操作标记在圈上。
第一步，先整体浏览一下程序结构，画出程序流程图。（草稿上画）
第二步，根据程序流程图就可以画出控制流图了。
(2)环路复杂度如何计算？
① 通过观察，流图中的区域数等于环形复杂度 4；（封闭区域+1）
② 环形复杂度等于 E-N + 2，其中 E 是流图中边的条数，N 是节点数；
③ 环形复杂度等于 P+1，P 是流图中判定节点(单逻辑)的数目，判定节点由流程图看出。
(3)程序的独立路径:这个就根据程序流图去写，从上到下，从左到右。]
(1)

(2)程序段的环形复杂度为 V(G) = 10 条边-8 个节点+2 = 4 (3) 路径 1：1→7→8          路径 2：1→2→6→8 路径 3：1→2→3→5→8   路径 4：1→2→3→4→8

3. 设一个控制流图如下，请给出环形复杂度和基本测试路径。
 ⑴根据程序环形复杂度的计算公式，求出程序路径集合中的独立路径数目。
公式 1: V(G)=11-9+2， 其中10 是控制流图 G 中边的数量，8 是控制流图中节点的数目。
公式 2: V(G)=3+1，其中 3 是控制流图 G 中判断节点的数目。
公式 3: V(G)=4，其中 4 是控制流图 G 中区域的数目。
因此，控制流图 G 的环形复杂度是 4。
(2)根据上面环形复杂度的计算结果，源程序的基本路径集合中有 4 条独立路径:
路径 1:5->22 路径 2:5->7，8->11，12->21->5->22 路径 3:5->7，8->16->17->19->21->5->22 路径 4:5->7，8->16->18->19->21->5->22

4. 使用基本路径测试方法,为以下程序段设计测试用例。
(1)画出程序的控制流图,编号已经给出。
(2)计算程序的循环复杂度，导出程序基本路径集中的独立路径条数。
(3)导出基本路径集,确定程序的独立路径。
(4)根据(3)中的独立路径， 设计测试用例(确保基本路径集中的每一条路径的执行)
的输入数据和预期输出。
void Do (int X, int A. int B)
{
1 if ((A>1)&& (B==0))
2  X = X/A;

3 if ( (A==2)||(X>1))
4  X=X+1;
5}
（1）由于控制流图假设的是单条件，因此对于复合条件,可将其分解为多个单个条件，并映射成控制流图。
1:A>1；2:B=0；3: X=X/A；4: A=2；5: X>1；6: X =X+1； 7: ]

（2）计算环形复杂度:10（条边)- 7(个节点)＋2= 5 （3）导出独立路径（用语句编号表示)
路径 1:1→2→3→4→5→6→7 路径 2:1→4→5→6→7 路径 3:1→2→4→6→7 路径 4:1→2→4→5→7 路径 5:1→2→3→4→5→7 （4）

5. 有一个求平均值的程序,最多允许输入 100 个值(以-999 为输入结束标志)，它可以计算落在给定范围内的那些值（称为有效输入值）的个数、总和及平均值。根据下面代码,用基本路径法进行测试,请写出其控制流图和路径集合以及测试用例。
float sum = 0,total = 0;
float average(value,mininum,maxinum)}{ float value[100];
 int mininum,maxinum;
 int inputnum=0,i=1;
 float aver;
 while(value[i]!=-999&&inputnum<100)
 {

inputnum++;
  if(value[i]>=mininum&&value[i]<=maxinum)
  {
   total++;
   sum=sum+value[i];
  }
  i++;
 }
 if(total>0)
  aver=sum/total;
 else aver=-999;
 return aver;}

v (G)=6(个区域)
v (G)=17〔条边）-13(个节点)+2=6 v (G)=5(个谓词节点）+1=6 确定独立路径集合(6 条)
路径 1:1-2-10-11-13(aloq)
路径 2:1-2-10-12-13( alnp)
路径 3:1-2-3-10-11-13(abmoq)
路径 4:1-2-3-4-5-8-9-2...... ( abcdfjk......)
路径 5:1-2-3-4-5-6-7-8-9-2...... ( abedchijk......)
路径 6:1-2-3-4-5-6-8-9-2...... ( abcdegjk......)
在本例中，判定节点(谓词节点)是节点 2、3、5、6 和节点 10。另外要注意路径

4、5、6 中节点 2 后面的省略号表示随后的部分已无关紧要。
设计测试用例，强制执行独立路径集中的每一条路径。

路径 1 的测试用例:
value(k)=有效输入，其中，k<i ; value (i)=-999，其中 2≤i≤100 期望结果:基于 k 的正确平均值的总数注意:路径Ⅰ必须作为路径 4、5、6 测试的一部分，它无法独立测试。
路径 2 的测试用例:
value(1)=-999;  期望结果: average=-999，其它保持初值路径 3 的测试用例:
试图处理 101 个或更多的值， 前100 个值应该有效。 期望结果与测试用例1 相同路径 4 的测试用例;
value (i)=有效输入，其中，i<100 ; Value (k)<最小值，其中 k<i

期望结果:根据有效输入值的个数和总数正确算出平均值路径 5 的测试用例:
value (i)=有效输入，其中,i<100 期望结果:根据有效输入值的个数和总数正确算出平均值路径 6 的测试用例:
value (i)=有效输入，其中,i<100 ; value (k)>最大值，其中 k<i 期望结果:根据有效输入值的个数和总数正确算出平均值

逻辑覆盖设计测试用例

6. 设计一组测试用例，要求分别满足语句覆盖、判定覆盖、条件覆盖。
int test(int A, int B)
{
if((A>1)AND (B<10)) then X=A-B;
if((A=2)OR (B>20)) then X=A+B;
return X;
)
答: 语句覆盖测试用例:A=2，B=0;
判定覆盖测试用例:A=3，B=0;A=2，B=20;
条件覆盖测试用例:A=2，B=0;A=0，B=21;

7. 设计一组测试用例，要求分别满足语句覆盖、判定覆盖、条件覆盖。
void DoWork (int x, int y,int z)
{
int k=0, j=0;
if ((x>3)&&(z<10))
{
k=x*y-1;
j=sqrt(k) ;
}
if ( (x==4)||(y>5))
{
j=x*y+10;
}
j=j%3;
}
①语句覆盖测试用例: x=4、y=5、z=5;程序执行路径：abd (只要满足那两个条件,设计一个测试用例就可以覆盖程序中的所有可执行语句。
语句覆盖可以保证程序中的每个语句都得到执行， 但发现不了判定中逻辑运算的错误，即它并不是一种充分的检验方法)

②判定覆盖测试用例: x=4、y=5、z=5; x=2、y=5、z=5;程序执行路径：bd;ce

③条件覆盖测试用例: x=4、y=6、z=5 ; (都真)
x=2、y=5、z=15 ;(都假)程序执行路径：bd;ce

(分析:上面这组测试用例不但覆盖了 4 个条件的全部 8 种情况，而且将两个判定的 4 个分支 b、c、d、e 也同时覆盖了，即同时达到了条件覆盖和判定覆盖。但是，并不是说满足条件覆盖就一定能满足判定覆盖。)

④判定/条件覆盖实际上是将判定覆盖和条件覆盖结合起来的一种方法。
只需设计以下两个测试用例便可以覆盖 4 个条件的 8 种取值以及 4 个判定分支。
x=4、y=6、z=5 ; (都真)
x=2、y=5、z=15 ;(都假)程序执行路径：bd;ce

⑤组合覆盖的目的是要使设计的测试用例能覆盖每一个判定的所有可能的条件取值组合。
对各个判定的条件取值组合加以标记:

1、x>3,z<10 记做 T1 T2，第一个判定的取真分支

2、x>3,z≥10 记做 T1 -T2，第一个判定的取假分支

3、x≤3,z<10 记做-T1 T2，第一个判定的取假分支

4、x≤3,z≥10 记做-T1-T2，第一个判定的取假分支

5、x=-4,y>5 记做 T3 T4，第二个判定的取真分支

6、x=-4, y≤5 记做 T3 -T4，第二个判定的取真分支

7、x!=4, y>5 记做-T3 T4，第二个判定的取真分支

8、x!=4, y≤5 记做-T3 -T4，第二个判定的取假分支

设计测试用例如下:

⑥路径覆盖：当程序中的每一条路径都受到了检验。
在满足组合覆盖的测试用例中修改其中一个测试用例，则可以实现路径覆盖:

分析:虽然前面一组测试用例满足了路径覆盖，但并没有覆盖程序中所有的条件组合 （丢失了组合3 和 7)） ， 即满足路径覆盖的测试用例并不一定满足组合覆盖。

请按照各种覆盖方法为下述语句设计测试用例，并写出测试过程 if ((A>1)&& (B==0))
  X = X/A;
if ((A==2)||(X>1))
  X=X+1;

语句覆盖:测试用例: A=2,B=0,X=3;程序执行路径：ace 判定覆盖:测试用例: a=2,b=0,x=3; a=1,b=0,x=1。则可分别执行路径 ace 和 abd 条件覆盖:测试用例:a=2,b=0,x=3(都真); a=1,b=1,x=1(都假)。 则可分别执行路径ace 和 abd 判定/条件覆盖:a=2,b=0,x=3(都真); a=1,b=1,x=1(都假)。则可分别执行路径 ace 和 abd 组合覆盖:测试用例:
对各个判定的条件取值组合加以标记:
1 号:a>1,b==0 记为 T1,T2 第一个判定的取真分支 2 号:a>1,b!=0 记为 T1,-T2 第一个判定的取假分支 3 号:a≤1,b=0 记做-T1 T2，第一个判定的取假分支 4 号:a≤1,b!=0 记做-T1-T2，第一个判定的取假分支 5 号:a=2,x>1 记做 T3 T4，第二个判定的取真分支 6 号:a=2,x>1 记做 T3 -T4，第二个判定的取真分支 7 号:a!=2,x>1 记做-T3 T4，第二个判定的取真分支 8 号:a!=2,x≤1 记做-T3 -T4，第二个判定的取假分支

路径覆盖:

8. 如图显示某程序的逻辑结构。试为它设计足够的测试用例，分别实现对程序的判定覆盖、条件覆盖和条件组合覆盖。
覆盖种类 需满足的条件 测试数据 预期结果判定覆盖 A>1,B=0 A=2,B=0 执行 S1 A>1,B≠0 或 A=2,B=1 或执行 S2 A≤1,B=0 或 A=1,B=0 或 A≤1,B≠0 A=1,B=1 条件覆盖以下四种情况各出现一次 A>1 B=0 A=2,B=0 执行 S1 A≤1 B≠0 A=1,B=1 执行 S2 条件组合 A>1,B=0 A=2,B=0 执行 S1 A>1,B≠0 A=2,B=1 执行 S2 A≤1,B=0 A=1,B=0 执行 S2 A≤1,B≠0 A=1,B=1 执行 S2

## 答案与解析（核查补充）

> 说明：本节为在原文基础上的核查补充。客观题优先采用原文答案或原文答案表；原文无明确答案且无法唯一推出时，标为需人工核对，避免误导。

### 第 2 题（主观题）

- 答案：参考答案：围绕题干要求作答，列出核心概念、步骤、适用条件和结论；案例题需结合材料指出做法、问题和改进措施。
- 核查：原文无明确答案，已补充评分点式参考答案；具体表格、图形或计算结果仍建议结合课堂标准答案人工复核。
- 解析：本题按课程常见定义、流程和方法核查，解析重点放在概念边界和易混选项。 题干关键词：使用基本路径测试方法测试以下程序段。

### 第 问答2 题（主观题）

- 答案：参考答案：围绕题干要求作答，列出核心概念、步骤、适用条件和结论；案例题需结合材料指出做法、问题和改进措施。
- 核查：原文无明确答案，已补充评分点式参考答案；具体表格、图形或计算结果仍建议结合课堂标准答案人工复核。
- 解析：本题按课程常见定义、流程和方法核查，解析重点放在概念边界和易混选项。 题干关键词：[ (1)方法:如何画控制流图？。

### 第 问答3 题（主观题）

- 答案：参考答案：围绕题干要求作答，列出核心概念、步骤、适用条件和结论；案例题需结合材料指出做法、问题和改进措施。
- 核查：原文无明确答案，已补充评分点式参考答案；具体表格、图形或计算结果仍建议结合课堂标准答案人工复核。
- 解析：本题按课程常见定义、流程和方法核查，解析重点放在概念边界和易混选项。 题干关键词：(2)环路复杂度如何计算？。

### 第 3 题（判断题）

- 答案：参考答案：原文未给出明确答案，需结合教材或课堂答案人工核对。
- 核查：原文无明确答案，未强行编造唯一选项。
- 解析：本题按课程常见定义、流程和方法核查，解析重点放在概念边界和易混选项。 题干关键词：设一个控制流图如下，请给出环形复杂度和基本测试路径。

### 第 4 题（选择题）

- 答案：参考答案：原文未给出明确答案，需结合教材或课堂答案人工核对。
- 核查：原文无明确答案，未强行编造唯一选项。
- 解析：覆盖准则强弱取决于是否覆盖更多判定、条件组合和路径，路径覆盖通常要求最高。 题干关键词：使用基本路径测试方法,为以下程序段设计测试用例。

### 第 6 题（主观题）

- 答案：参考答案：围绕题干要求作答，列出核心概念、步骤、适用条件和结论；案例题需结合材料指出做法、问题和改进措施。
- 核查：原文无明确答案，已补充评分点式参考答案；具体表格、图形或计算结果仍建议结合课堂标准答案人工复核。
- 解析：覆盖准则强弱取决于是否覆盖更多判定、条件组合和路径，路径覆盖通常要求最高。 题干关键词：设计一组测试用例，要求分别满足语句覆盖、判定覆盖、条件覆盖。

### 第 7 题（判断题）

- 答案：参考答案：原文未给出明确答案，需结合教材或课堂答案人工核对。
- 核查：原文无明确答案，未强行编造唯一选项。
- 解析：覆盖准则强弱取决于是否覆盖更多判定、条件组合和路径，路径覆盖通常要求最高。 题干关键词：设计一组测试用例，要求分别满足语句覆盖、判定覆盖、条件覆盖。

### 第 8 题（主观题）

- 答案：参考答案：围绕题干要求作答，列出核心概念、步骤、适用条件和结论；案例题需结合材料指出做法、问题和改进措施。
- 核查：原文无明确答案，已补充评分点式参考答案；具体表格、图形或计算结果仍建议结合课堂标准答案人工复核。
- 解析：覆盖准则强弱取决于是否覆盖更多判定、条件组合和路径，路径覆盖通常要求最高。 题干关键词：如图显示某程序的逻辑结构。试为它设计足够的测试用例，分别实现对程序。
